mixin video(video)
    div
            h4
                a(href=`/videos/${video.id}`)=video.title
            ul 
                li #{video.ration} / 5.
                li #{video.comments} commets.
                li Posted #{video.creadtedAt}.
                li #{video.views} views.